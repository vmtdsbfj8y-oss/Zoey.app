/**
 * Phantom route backing the center FAB slot in the tab bar. Never rendered --
 * the FAB's custom tabBarButton intercepts the press and opens /chat.
 */
export default function FabSlot() {
  return null;
}
